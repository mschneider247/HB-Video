import * as path from "path";

export const PORT            = 3000;
export const MONSTERS_DIR    = path.join(__dirname, "..", "habitBeast", "public");
export const OUTPUT_DIR      = __dirname;
export const GOOD_TO_GO_DIR  = path.join(OUTPUT_DIR, "GoodToGo");
export const SCREENSHOTS_DIR = path.join(OUTPUT_DIR, "Screenshots");
export const COMPOSED_DIR    = path.join(OUTPUT_DIR, "Composed");
